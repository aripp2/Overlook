

  it('should be able to purchase room service', () => {

  });

  it('should be able to calculate a total bill', () => {
    // for given date?
  });
// dashboard or main?
  it('should display today\'s date', () => {
    // DOM?
  });

  it('should return total num room available today', () => {

  });

  it('should return total revenue for today', () => {

  });

  it('should return percent of rooms occupied today', () => {

  });
// customer
  it('should be able to create new customer', () => {
    // Only enter name in DOM
  });
// orders
  it('should return all room service orders for today', () => {
    //default
  });

  it('should be able to search for all orders specific date', () => {
    // default
  });

  it('should return all orders with date and amount', () => {
    // selected customer
  });

  it('should return the amount spent on room service today', () => {
    // selected customer
  });

  it('should return all time total spent on room service', () => {
    // selected customer
  });

  it('should return notification if no data exists', () => {
    // DOM
  });
// Rooms
  it('should return the date with the most bookings', () => {
    // Default- 'Most Popular Booking Date'
  });

  it('should return the date with most rooms available', () => {
    // Default - future or all?
  });

  it('should return rooms available on given date', () => {
    // DOM search input
  });
// Selected Customer
  it('should return all past and current bookings', () => {
    // Summary?? 
  });

  it('should return a notification if no data exists', () => {
    // DOM
  });

  it('should allow user to create a new booking if not one for today', () => {
    // DOM - button- will reaveal a menu for filtering rooms by type
  });

  it('should be able to filter available rooms by type', () => {

  });

  it('should return all details for desired room', () => {

  });

  it('should return all available rooms if desired is unavailable', () => {

  });

  it('should return the type, numBeds, bedSize and if bidet of available rooms', () => {
    // info to be available before completing the booking
  });

  it('should have option to add room service after making booking', () => {
    // button to add room service will reveal a menu, list of food and prices
  });

  it('should return changes and charges for room service on orders tab', () => {

  });

